{
  "name": "brainlink",
  "private": true,
  "engines": {
    "node": ">=20 <23"
  },
  "scripts": {
    "refined:install": "npm --prefix frontend_refined install",
    "refined:dev": "npm --prefix frontend_refined run dev",
    "refined:build": "npm --prefix frontend_refined run build",
    "refined:lint": "npm --prefix frontend_refined run lint",
    "refined:preview": "npm --prefix frontend_refined run preview",
    "build": "node scripts/build-ui.js --refined",
    "build:refined": "node scripts/build-ui.js --refined"
  }
}
