## Node API
PORT=8081
NODE_ENV=development
JWT_SECRET=change-me
CORS_ORIGINS=http://localhost:3000

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=signup

PYTHON_BASE_URL=http://localhost:8082

## Python API (Flask)
# These can live in the same file if you run both services locally.
# If you deploy them separately, set env vars per-service instead.
PY_PORT=8082
FAISS_DIR=faiss_index
GOOGLE_API_KEY=
HUGGINGFACEHUB_API_TOKEN=
mongodb+srv://vohratanya5_db_user:px2mtsGrzihQlena@cluster0.c02hzhr.mongodb.net/?BrainLink=Cluster0
